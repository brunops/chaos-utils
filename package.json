{
  "name": "chaos-utils",
  "version": "1.3.0",
  "description": "chaos utils functions - from \"playing with chaos\" book",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel --out-dir dist index.js",
    "prepublish": "npm run compile"
  },
  "author": "brunops",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.14.0",
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
